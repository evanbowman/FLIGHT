#include "Font.hpp"

namespace FLIGHT {    
    // static FT_Library ft;

    void FontFace::Init() {
	// if (FT_Init_FreeType(&ft)) {
	// throw std::runtime_error("failed to initialize freetype");
	//}
    }
}
